# @package _global_

# EfficientTAM-Ti variant 1: tiny base with memory cross-attention = EfficientRoPEAttention1.
defaults:
  - efficienttam_ti_base
  - _self_

model:
  memory_attention:
    layer:
      cross_attention:
        _target_: sam.modeling.decoders.transformer.EfficientRoPEAttention1
