# @package _global_
# SPDX-License-Identifier: LicenseRef-SAM
# EfficientSAM3 TinyViT-11M base-lineage (non-multiplex) VIDEO predictor.
# Video base with the vision trunk swapped to TinyViT-11M.
# Checkpoint: stage1_all_converted/efficient_sam3_tinyvit_m_geo_ft.pt.
defaults:
  - efficientsam3_video_base
  - _self_

model:
  vision_encoder:
    vision_backbone:
      trunk:
        backbone_type: tinyvit
        model_name: 11m
